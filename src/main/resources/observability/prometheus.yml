# prometheus.yml (Prometheus scrapes your app directly, alongside Mimir getting data via OTLP)
scrape_configs:
  - job_name: my-app
    metrics_path: /actuator/prometheus
    static_configs:
      - targets: ["host.docker.internal:8081"]
