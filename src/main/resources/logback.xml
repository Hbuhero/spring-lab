<?xml version="1.0" encoding="UTF-8"?>
<configuration scan="false">

    <include resource="org/springframework/boot/logging/logback/defaults.xml"/>

    <springProperty
            scope="context"
            name="appName"
            source="spring.application.name"
            defaultValue="my-app"/>

    <!-- ===================== JSON Console Appender =====================
         Structured JSON logs to stdout.
         Useful for Kubernetes/container environments and can also be
         consumed by Loki/Promtail, Filebeat, etc. -->
    <appender name="CONSOLE_JSON" class="ch.qos.logback.core.ConsoleAppender">
        <encoder class="net.logstash.logback.encoder.LogstashEncoder">

            <includeMdcKeyName>traceId</includeMdcKeyName>
            <includeMdcKeyName>spanId</includeMdcKeyName>

            <customFields>{"service":"${appName}"}</customFields>

            <fieldNames>
                <timestamp>@timestamp</timestamp>
                <message>message</message>
                <logger>logger</logger>
                <thread>thread</thread>
                <level>level</level>
            </fieldNames>

        </encoder>
    </appender>


    <!-- ===================== OpenTelemetry Appender =====================
         Sends application logs through OpenTelemetry.
         The OpenTelemetry Collector can then route them to Loki
         or another LGTM backend. -->
    <appender name="OTEL"
              class="io.opentelemetry.instrumentation.logback.appender.v1_0.OpenTelemetryAppender">

        <captureExperimentalAttributes>true</captureExperimentalAttributes>
        <captureCodeAttributes>true</captureCodeAttributes>
        <captureMdcAttributes>*</captureMdcAttributes>

    </appender>


    <!-- ===================== Async JSON ===================== -->

    <appender name="ASYNC_JSON"
              class="ch.qos.logback.classic.AsyncAppender">

        <queueSize>512</queueSize>
        <discardingThreshold>0</discardingThreshold>
        <neverBlock>true</neverBlock>

        <appender-ref ref="CONSOLE_JSON"/>

    </appender>


    <!-- ===================== Async OpenTelemetry ===================== -->

    <appender name="ASYNC_OTEL"
              class="ch.qos.logback.classic.AsyncAppender">

        <queueSize>512</queueSize>
        <discardingThreshold>0</discardingThreshold>
        <neverBlock>true</neverBlock>

        <appender-ref ref="OTEL"/>

    </appender>


    <!-- ===================== Root Logger =====================
         LGTM configuration is always active. -->

    <root level="INFO">
        <appender-ref ref="ASYNC_JSON"/>
        <appender-ref ref="ASYNC_OTEL"/>
    </root>

</configuration>